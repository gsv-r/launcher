import Layout from "../components/frame/layout";

export default function Profile(): React.JSX.Element {
  return (
    <Layout>
      <p className="text-neutral-500 text-md">This is the profile screen.</p>
    </Layout>
  );
}